PORT=3000
AUTO_OPEN=0

# Opción 1: Cadena de conexión completa (ideal en Supabase/Render)
# DATABASE_URL=postgresql://usuario:password@host:puerto/base

# Opción 2: Variables por separado
DB_HOST=localhost
DB_USER=postgres
DB_PASSWORD=tu_password
DB_NAME=historias_clinicas
DB_PORT=5432

# SSL (por defecto activo). Para local puedes desactivar con NO_SSL=true
# NO_SSL=false
# PGSSLMODE=no-verify

# JWT Configuration (Autenticación con JSON Web Tokens)
JWT_SECRET=tu_jwt_secret_muy_seguro_cambiar_en_produccion
JWT_EXPIRES_IN=1h
JWT_REFRESH_SECRET=tu_refresh_secret_muy_seguro_cambiar_en_produccion
JWT_REFRESH_EXPIRES_IN=7d

NODE_ENV=development
